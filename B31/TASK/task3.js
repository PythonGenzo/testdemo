console.log("hai");

const scores = [
    {
      marks: 32,
      name: "Yvette Merritt"
    },
    {
      marks: 57,
      name: "Lillian Ellis"
    },
    {
      marks: 22,
      name: "Mccall Carter"
    },
    {
      marks: 21,
      name: "Pate Collier"
    },
    {
      marks: 91,
      name: "Debra Beard"
    },
    {
      marks: 75,
      name: "Nettie Hancock"
    },
    {
      marks: 20,
      name: "Hatfield Hodge"
    }
  ];

//   1) Found Average Marks in yhe class ?

//   const sum = scores.filter(marks);
//   const marks = sum.map(getMarks);
//   const total = marks.reduce(addMarks, 0);
//   const average = total / marks.length;
//   console.log("average: ", average);

    //  const average = 
    //  scores
    //     .map((scores) => scores.marks);
    //     .reduce((scores, marks) => scores + marks, 0);
    // console.log(average);   
    
    
    
    // 2) Found the topper name in the class ?

    // const topper = scores.filter ((score) => scores.marks);
    // console.log(topper);
